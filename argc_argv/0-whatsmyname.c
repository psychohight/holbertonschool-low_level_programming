#include <stdio.h>

/**
 * main - check the code
 * @argc: nbr
 * @argv: nbr
 *
 * Return: Always o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
